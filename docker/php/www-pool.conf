; Override the stock pool (only 5 children) so a few browser tabs +
; live polling don't exhaust the worker pool.
[www]
pm = dynamic
pm.max_children = 25
pm.start_servers = 5
pm.min_spare_servers = 3
pm.max_spare_servers = 10
pm.max_requests = 500
